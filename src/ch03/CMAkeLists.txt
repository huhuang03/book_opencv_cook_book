SINGLE_EXE(flood_fill)
SINGLE_EXE(grab_cut)
SINGLE_EXE(hsv_color)
SINGLE_EXE(hsv_color_v_to_255)
SINGLE_EXE(detect_hsv_color)
add_executable(detect_sky detect_sky.cc color_detector.cc)
target_link_libraries(detect_sky ${OpenCV_LIBS})

set(IMAGES ${IMAGE_DIR}/boldt.jpg)
set(IMAGES ${IMAGE_DIR}/girl.jpg)
FILE(COPY ${IMAGES} DESTINATION .)
FILE(COPY ${IMAGES} DESTINATION Debug)
FILE(COPY ${IMAGES} DESTINATION Release)